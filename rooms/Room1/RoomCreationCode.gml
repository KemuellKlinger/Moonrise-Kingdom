audio_stop_all();
// No evento "Create" de um controlador ou da sala:
//instance_create_layer(500, room_height - 100, "Instances", obj_ataque);
room_persistent = true;
global.sala_anterior = noone; // Inicializa sem sala salva
